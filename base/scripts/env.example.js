// Plantilla de variables de entorno del front.
// Copiá este archivo como `env.js` (mismo directorio) y completá tu API key.
// `env.js` está en .gitignore: nunca se sube al repositorio.
//
// IMPORTANTE: en un sitio estático sin backend, esta key queda visible en el
// navegador en tiempo de ejecución. Este patrón sirve para mantenerla fuera
// del repositorio, NO para ocultarla del usuario final. Para secreto real
// haría falta un backend/proxy, fuera del alcance de este proyecto.
window.MINI_FINANCE_ENV = {
  FASTFOREX_API_KEY: 'TU_API_KEY_AQUI',
};
